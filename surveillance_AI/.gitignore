# Part 1 local state / large artifacts (root .gitignore also covers *.pt, storage/)

# model weights live here (downloaded/bundled, not committed)
models/*.pt
models/*.pth
models/*.ckpt
models/*.onnx
models/**/*.zip
models/models/          # stray insightface buffalo_l downloads (not used)

# feature_id local gallery + saved snapshots (runtime state, not source)
feature_id/data/
