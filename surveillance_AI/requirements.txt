# Part 1 — Perception pipeline dependencies.
#
# torch / torchvision: install the build that matches your machine/GPU from
#   https://pytorch.org/get-started/locally/  (CPU wheels are the default below).
# SAM 2 is NOT on PyPI as `sam2` for all versions — install from the SAM 2 repo
#   (see README: "Segmentation (SAM 2) setup"). Only needed for --segment / masks.
# torchreid provides the OSNet body-ReID model used by feature_id/extractor.py.

torch
torchvision
opencv-python
numpy
pillow               # image handling for the RT-DETR image processor
requests
transformers         # RT-DETR person detector (default detector; not YOLO)
torchreid            # OSNet body ReID (fallback identity)
facenet-pytorch      # MTCNN face detector (recognition is AdaFace, vendored in feature_id/adaface_net.py)
gdown                # to fetch the OSNet + AdaFace weights (see README "Weights")
boxmot==19.0.0       # OC-SORT motion tracker (explicit backend — no silent IoU fallback)
pytest               # tracker scenario tests (tests/)
